'use client'
import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    kakao: {
      main: '#FFD700', // 노란색
    },
    naver: {
      main: '#069729', // 초록색
    },
  },
})

export default theme
