'use client'
import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    yellow: {
      main: '#FFD700', // 노란색
    },
    green: {
      main: '#069729', // 초록색
    },
  },
})

export default theme
